[project]
name = "news_recommendation_tc"
authors = [{name = "", email = ""}]
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests"
]

[project.optional-dependencies]
dev = [
    "pytest"
]
